import React from "react";
import styled from "@emotion/styled";
import { Box, Button } from "@mui/material";

export const Login: React.FC = () => {
  const styledBox = styled(Box)({
    margin: "auto auto",
  });

  return (
    <styledBox>
      <Button variant="contained" size="large" href=""></Button>
    </styledBox>
  );
};
